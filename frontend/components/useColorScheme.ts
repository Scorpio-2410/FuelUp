/**
 * Color scheme hook that provides access to the device's current theme preference.
 * Re-exports React Native's useColorScheme hook for consistent usage across the app.
 * Returns 'light', 'dark', or null based on the user's system theme settings.
 */
export { useColorScheme } from 'react-native';
